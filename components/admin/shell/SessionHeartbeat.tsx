"use client";

/**
 * Placeholder. The sessions step makes this poll /api/auth/session-status every
 * 30 seconds and on tab focus, and send a revoked browser to the login page
 * (docs/plan/admin-cms-adr.md, section 6.2).
 */
export default function SessionHeartbeat() {
  return null;
}
